/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import type { SceneDocument, ScenePatternDistribution, SceneTransform } from './types.js';
export declare const MAX_SCENE_PATTERN_INSTANCES = 10000;
export interface GenerateScenePatternOptions {
    collisionRadius?: number;
    maxInstances?: number;
    seedKey?: string;
}
export interface SceneRuntimeNodeIdEntry {
    derived: boolean;
    id: string;
    sourcePath: string;
}
/**
 * Expand a pattern in stable order. Scatter uses the declared PCG32
 * algorithm; other distributions derive a stable seed from the scene data.
 */
export declare function generateScenePatternTransforms(distribution: ScenePatternDistribution, options?: GenerateScenePatternOptions): SceneTransform[];
export declare function getScenePatternRequestedCount(distribution: ScenePatternDistribution): number;
/** Actual materialized instance count for the supplied deterministic options. */
export declare function getScenePatternInstanceCount(distribution: ScenePatternDistribution, options?: GenerateScenePatternOptions): number;
/** Stable ID for a local prefab node expanded by an instance node. */
export declare function deriveSceneInstanceNodeId(instanceNodeId: string, prefabNodeId: string): string;
/** Stable namespace for one expanded pattern instance. */
export declare function deriveScenePatternInstanceNamespace(patternNodeId: string, instanceIndex: number): string;
/** Stable ID for a local prefab node expanded by a pattern instance. */
export declare function deriveScenePatternNodeId(patternNodeId: string, instanceIndex: number, prefabNodeId: string): string;
/**
 * Enumerate authored and reserved derived runtime IDs using the exact same
 * namespace rules as lowering. Scatter reserves its declared contiguous ID
 * range even when collision skipping materializes fewer instances.
 */
export declare function collectSceneRuntimeNodeIds(document: SceneDocument, maxDerivedNodes?: number): SceneRuntimeNodeIdEntry[];
//# sourceMappingURL=expansion.d.ts.map