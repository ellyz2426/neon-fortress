import { baseOutPropertiesSchema, createInPropertiesSchema, defineSchema } from '@pmndrs/uikit';
import { Container } from '@pmndrs/uikit';
export const ProgressBarStepperOutPropertiesSchema = baseOutPropertiesSchema;
export const ProgressBarStepperPropertiesSchema = /* @__PURE__ */ defineSchema(() => createInPropertiesSchema(ProgressBarStepperOutPropertiesSchema));
export class ProgressBarStepper extends Container {
    constructor(inputProperties, initialClasses, config) {
        super(inputProperties, initialClasses, {
            ...config,
            defaultOverrides: {
                flexDirection: 'row',
                height: 12,
                gap: 8,
                ...config?.defaultOverrides,
            },
        });
    }
}
