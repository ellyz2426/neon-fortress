import { object, string } from 'zod';
import { baseOutPropertyShape, createInPropertiesSchema, defineSchema } from '@pmndrs/uikit';
import { Text, } from '@pmndrs/uikit';
import { computed } from '@preact/signals-core';
import { Dropdown } from './index.js';
export const DropdownTextValueOutPropertiesSchema = /* @__PURE__ */ defineSchema(() => object({
    ...baseOutPropertyShape,
    placeholder: string().optional(),
}).strict());
export const DropdownTextValuePropertiesSchema = /* @__PURE__ */ defineSchema(() => createInPropertiesSchema(DropdownTextValueOutPropertiesSchema));
export class DropdownTextValue extends Text {
    constructor(inputProperties, initialClasses, config) {
        const text = computed(() => {
            const dropdown = this.parentContainer.value;
            if (dropdown instanceof Dropdown && dropdown.currentSignal.value != null) {
                return dropdown.currentSignal.value;
            }
            return this.properties.value.placeholder;
        });
        super(inputProperties, initialClasses, {
            ...config,
            defaultOverrides: {
                text,
                ...config?.defaultOverrides,
            },
        });
    }
}
