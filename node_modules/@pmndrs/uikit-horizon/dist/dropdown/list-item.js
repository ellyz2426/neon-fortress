import { object, string } from 'zod';
import { baseOutPropertyShape, createInPropertiesSchema, defineSchema } from '@pmndrs/uikit';
import { Container } from '@pmndrs/uikit';
import { Dropdown } from './index.js';
export const DropdownListItemOutPropertiesSchema = /* @__PURE__ */ defineSchema(() => object({
    ...baseOutPropertyShape,
    value: string().optional(),
}).strict());
export const DropdownListItemPropertiesSchema = /* @__PURE__ */ defineSchema(() => createInPropertiesSchema(DropdownListItemOutPropertiesSchema));
export class DropdownListItem extends Container {
    constructor(inputProperties, initialClasses, config) {
        super(inputProperties, initialClasses, {
            ...config,
            defaultOverrides: {
                paddingY: 8,
                paddingX: 16,
                onClick: (event) => {
                    const dropdown = this.parentContainer.value?.parentContainer.value;
                    if (!(dropdown instanceof Dropdown)) {
                        return;
                    }
                    const value = this.properties.peek().value;
                    dropdown.uncontrolledSignal.value = value;
                    dropdown.properties.peek().onValueChange?.(value);
                },
                ...config?.defaultOverrides,
            },
        });
    }
}
